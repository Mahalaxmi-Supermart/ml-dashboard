/** `type` on POST .../medias/ (e.g. primary product image). */
export const MEDIA_RECORD_TYPE = {
  IMAGE: 1,
} as const

/** `status` on POST .../medias/ */
export const MEDIA_RECORD_STATUS = {
  ACTIVE: 1,
} as const
